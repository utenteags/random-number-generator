{
  "name": "random-number-generator",
  "version": "1.0.0",
  "description": "A fullstack random number generator project build for pippo",
  "main": "main.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "enalotto",
    "random",
    "number",
    "generator"
  ],
  "author": "@pippo",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.1.0"
  }
}
